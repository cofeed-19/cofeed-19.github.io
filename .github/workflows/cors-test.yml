name: CORS Test
on: [push]
jobs:
  Test-CORS:
    runs-on: ubuntu-latest
    steps:
      - run: chmod +x scripts/cors-test.sh
      - run: chmod +x scripts/test-feed-data.sh
      - run: bash scripts/cors-test.sh